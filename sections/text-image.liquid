<div id="{{section.id}}">
    <div>
        <h2>
            {{section.settings.heading}}
        </h2>
        <div>
            {{section.settings.description}}
        </div>
        <a href="{{section.settings.url}}">
            {{section.settings.urlText}}
        </a>
    </div>
    {% if section.settings.image %}
        <div>
            {% render 'basic-responsive-image', type: block.settings.image, data_widths: '[180, 360, 540, 720, 900]' %}
              <noscript>
                <img         loading="lazy"
                width="{{ section.settings.image.width }}"
                height="{{ section.settings.image.height }}" src="{{ section.settings.image | img_url: '800x' }}" alt="{{ section.settings.image.alt }}">
              </noscript>
        </div>>
    {% endif %}
</div>


{% schema %}

{
    "name": "Text with Image",
    "settings": [
        {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
        },
        {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Here We Are"
        }, {
            "type": "richtext",
            "id": "description",
            "label": "Description",
            "default": "<p>Our team love great furniture. Part of that passion means making it available to everyone at affordable prices. From developers to designers, or customer service reps to quality engineers, each branch of the Interior Icons team is committed to producing the best furniture at incredible prices. Our global family takes in diverse skills and attitudes, but we all share a love of iconic furniture and the desire to make it accessible to all.</p>"
        }, {
            "type": "url",
            "id": "url",
            "label": "Url",
            "default": "/"
        }, {
            "type": "text",
            "id": "urlText",
            "label": "Button Text",
            "default": "Learn More"
        }
    ]
}

{% endschema %}